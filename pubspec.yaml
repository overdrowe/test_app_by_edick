name: test_app_by_auramel
description: Test app for grading

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter


  cupertino_icons: ^1.0.2
  get: ^4.3.8
  flutter_svg: ^0.22.0
  pull_to_refresh: ^2.0.0
  cached_network_image: ^3.1.0
  photo_view: ^0.12.0

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true

  assets:
    - assets/icons/
    - assets/logo/logo.svg























  #
  
  
